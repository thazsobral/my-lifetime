function orchestrator () {
  setInterval(function () {
    init ()
  }, 1000)
}

function init () {
  // quanto tempo já vivi (anos, meses, semanas, dias)?
  
  // quantos anos já vivi?
  // quantos meses já vivi?
  // quantos semanas já vivi?
  // quantos dias já vivi?

  // que dia da semana eu nasci?
}
